请以资深测试/质量工程师与 C++/Qt 网络栈专家的视角，参考并严格遵循 `tests/libcurl_consistency/README.md` 与 `tests/libcurl_consistency/tasks.md` 的既有目标、术语、范围边界与产出格式，对整个项目重新做一次端到端、可复现的完整分析（包含源码结构、现有测试覆盖、关键数据流、可观测输出、运行方式与限制条件）。在分析过程中必须以“可观测数据层面一致性”为唯一判定核心：即 QCurl 与 libcurl 在不依赖实现细节、仅基于外部可观察结果（例如：回调/事件序列、状态码与错误码、header/body 字节级内容、重定向与重试行为、TLS/代理/HTTP2/连接复用等对外表现、时间/超时与取消语义、进度与统计信息、日志与诊断输出、资源释放与生命周期可见信号）的对齐程度。

完成分析后，基于证据与差距清单，按需更新 `tests/libcurl_consistency/README.md` 与 `tests/libcurl_consistency/tasks.md`，更新要求：
- 只修改确有必要的内容，保持现有文档风格与结构一致；任何新增条目必须可执行、可验证、可追踪，并明确验收标准。
- 在 `README.md` 中补齐/修正：测试目的与非目标、术语定义（尤其是“可观测数据”的边界）、测试架构与数据采集方式、运行前置条件、如何判定一致/不一致、常见陷阱与已知限制、失败示例与定位路径（如适用）。
- 在 `tasks.md` 中以任务项形式输出缺口与改进：每个任务必须包含背景、覆盖的 libcurl API / QCurl 行为点、输入场景、期望可观测输出、对比方式、边界条件、优先级、完成判据与最小可复现步骤；避免泛泛而谈，避免仅描述实现方案而缺少可观测验收。
- 明确判断当前是否缺失“QCurl 与 libcurl API 在可观测数据层面一致性”的测试：给出覆盖矩阵/清单（按协议层/功能点/错误路径/时序语义分类），指出已覆盖项、部分覆盖项与缺失项，并解释判定依据（对应到代码/测试/文档位置）。
- 若发现存在“看似一致但在可观测层面可被区分”的风险点（例如 header 规范化差异、重定向次数与顺序、超时触发时机、回调触发顺序、取消后仍产生数据事件、进度统计口径差异、HTTP/2 多路复用下时序差异、TLS 握手失败映射差异等），必须在文档中显式记录并提出具体测试任务来验证。

输出要求：
- 你的最终输出只包含对 `tests/libcurl_consistency/README.md` 与 `tests/libcurl_consistency/tasks.md` 的更新后完整内容（如无需修改则原样重述并说明“无需修改”的结论必须体现在文档正文中），不得输出对话、解释过程或额外文件清单。
- 全程以可复现性为标准：任何新增测试/任务描述必须让读者在不知道你思考过程的情况下也能直接照做并得到可对比的可观测结果。